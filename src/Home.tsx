//create an homepage
const Home = () => {
    return (
        <div style={{ display: "flex", minHeight: "100vh" }}>
            <h1>Bienvenido a la Home</h1>
        </div>
    );
};

export default Home;